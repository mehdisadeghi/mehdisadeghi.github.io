box: debian
build:
  steps:
    - arjen/hugo-build:
        version: 0.14
        theme: hugo-uno
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: mehdisadeghi.me
        basedir: public
